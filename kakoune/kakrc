# Show autoinfo on normal mode inputs
set-option -add global autoinfo normal

# Bind /\c to /(?i) to satisfy my Vim habits
# TODO: fix this, it bork
# map global normal '/\c' '/(?i)' -docstring 'search, case insensitive'
